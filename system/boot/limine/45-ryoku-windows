#!/usr/bin/env bash
#
# limine-entry-tool post.d hook. Re-asserts Windows and any installer-recorded
# neighboring ESP after menu regeneration. No-op when absent; never fatal.
command -v ryoku-windows-entry >/dev/null 2>&1 || exit 0
ryoku-windows-entry sync >/dev/null 2>&1 || true
exit 0
